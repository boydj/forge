Node age recipients (public keys), one `<pop>.age.pub` per node, collected by
`scripts/deploy init-node <pop>` from `/etc/forge/age.pub` on the node. Safe to
commit. The private halves never leave the nodes.
